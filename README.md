This lab introduces concepts such as atomics, gather and scatter.
Make a program that calculates in parallel, four threads, two stage tone mapping for a
BMP file. Have a gather function in the two steps. Use a DIY solution as well as a
sense-reversing barrier. Show that both work. Use shared mem, no TCP-IP.
